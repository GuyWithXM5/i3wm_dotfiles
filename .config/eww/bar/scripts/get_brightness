#!/bin/sh
# Output brightness percentage (0-100). Always prints a number for eww label.
out=$(brightnessctl -m 2>/dev/null | head -1)
if [ -n "$out" ]; then
	printf '%s' "$out" | awk -F',' '{ gsub(/%/, "", $4); print $4 "%" }'
else
	echo "0%"
fi
